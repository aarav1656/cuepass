fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
httpx>=0.27.0
parallel-web>=1.3.0
# The run is a google.adk.workflow.Workflow graph. 2.8 is the first version
# exposing Workflow/FunctionNode/JoinNode, which is what the topology is built
# from; SequentialAgent and ParallelAgent carry a deprecation notice there.
google-adk>=2.8.0
google-genai>=1.12.0
pydantic>=2.7.0
pytest>=8.0.0
